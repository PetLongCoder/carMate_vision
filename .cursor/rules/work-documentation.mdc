---
description: 工作说明文档默认 .docx 格式与命名规范（罗天赐）
alwaysApply: true
---

# 工作说明文档规范

用户要求整理「今天完成的工作」时，按以下约定生成文档。

## 格式

- **默认 `.docx`**，除非用户明确要求其他格式（如 `.md`、`.pdf`）
- 使用 `python-docx` 生成，样式参考 `backend/scripts/generate_daily_report_*.py`

## 命名

- 文件名 = **当天完成工作的主题 + `说明`**
- 示例：控制面板 → `控制面板实现说明.docx`；告警中心 → `告警中心改造说明.docx`
- **不要**用日期前缀或 `CarMate_今日工作完成说明_YYYYMMDD` 这类通用名

## 输出位置

- 默认保存到桌面：`C:\Users\18151\Desktop\{主题}说明.docx`

## 内容

- 标题与文件名主题一致（如「控制面板实现说明」）
- 含：成果摘要、实现细节、关键文件、Git 状态、验证方式
- 用户未要求时，**不要**额外写 Markdown 版

## 脚本

- 可新建 `backend/scripts/generate_*_说明.py`，脚本内 `OUTPUT` 与文档标题与主题一致
