# 后端 API 地址（与 vite.config.ts 代理、start_all.bat 一致，端口 8000）
VITE_API_BASE_URL=http://localhost:8000/api

# WebSocket 地址
VITE_WS_URL=ws://localhost:8000

# 连接云数据库时请设为 false；不设或 false 均走真实后端（clone 后默认可用）
# 仅本地纯前端演示时设为 true
# VITE_USE_MOCK_AUTH=true

# 后端 WebSocket 就绪后改为 true
VITE_ENABLE_WS=true
